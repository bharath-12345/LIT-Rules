package com.myspace.lit;

import com.myspace.lit.Employee;

no-loop

rule 'New Joinee'
when
    $emp: Employee(noDaysSinceJoining == 0);
then
    modify($emp) {
        setJobRole("Reset jobRole");
    }
end